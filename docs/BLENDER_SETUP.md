# Установка Blender + экспортёра i3d для FS19

Пошагово, с граблями, на которые все наступают.

---

## Ссылки (всё бесплатное, ничего покупать не надо)

| Что | Ссылка |
|---|---|
| **Blender** — последняя версия | https://www.blender.org/download/ |
| **Blender 4.2 LTS** — если понадобится откат | https://download.blender.org/release/Blender4.2/ (файл `blender-4.2.19-windows-x64.msi` или новее в этой папке) |
| **Экспортёр i3d** — страница релизов | https://github.com/StjerneIdioten/I3D-Blender-Addon/releases/latest |
| **Экспортёр i3d** — прямая ссылка на файл | https://github.com/StjerneIdioten/I3D-Blender-Addon/releases/download/v6.2.0/i3d_exporter-6.2.0.zip |
| Документация экспортёра | https://stjerneidioten.github.io/I3D-Blender-Addon/ |
| GIANTS Editor (SDK) | https://gdn.giants-software.com/downloads.php (нужна бесплатная регистрация) |

---

## Шаг 1. Blender

Скачайте последнюю версию с blender.org и поставьте **от имени администратора**.

Актуальный экспортёр (v6.2.0, май 2026) идёт в ногу с Blender — в его списке
изменений есть даже поддержка API Blender 5.0. Так что начинайте с последней
версии.

**Если после установки аддона панель GIANTS I3D не появится** — это классическая
несовместимость версий. Тогда поставьте **Blender 4.2 LTS** рядом (несколько
версий Blender спокойно живут вместе) и повторите установку аддона в ней.

---

## Шаг 2. Экспортёр

Берём версию от сообщества (StjerneIdioten), а не официальный от GIANTS:
он обновляется регулярно, ставится обычным zip и не требует регистрации.
Официальный идёт .exe-инсталлятором, который часто ставится криво — на GDN
стандартный совет звучит как «жмите Extract files и ставьте zip руками».

1. Скачайте **`i3d_exporter-6.2.0.zip`** — либо прямой ссылкой из таблицы выше,
   либо на странице релиза раскройте **Assets** и возьмите файл оттуда.
   Не «Source code (zip)» — это другое.
2. Положите zip в постоянное место (например `Документы\Blender\addons\`),
   не в Загрузки.
3. Blender → `Edit → Preferences → Add-ons`.
   В Blender 4.2+ кнопка установки спрятана: стрелочка **▾** справа сверху →
   **Install from Disk**.
4. Выберите скачанный zip.
5. Включите галочку у **«Unofficial GIANTS I3D Exporter Tools»**.
   Не находится — переключите фильтр категории на **Community**.
6. Разверните аддон и в поле **FS Data Folder** укажите папку `data` игры:
   - Steam: `C:\Program Files (x86)\Steam\steamapps\common\Farming Simulator 19\data\`
   - Диск/Giants: `C:\Program Files (x86)\Farming Simulator 19\data\`
7. Внизу слева ☰ → **Save Preferences**.

**Не распаковывайте zip руками** в папку addons — аддону важно точное имя папки,
кнопка установки делает это правильно.

---

## Шаг 3. Проверка, что всё встало

1. В окне 3D Viewport нажмите **`N`** — справа выедет боковая панель.
2. Там должна появиться вкладка **GIANTS I3D**.

Если вкладки нет — версия экспортёра не подходит к вашему Blender.
Поставьте Blender 4.2 LTS и повторите установку аддона в нём.

Ещё полезно открыть `Window → Toggle System Console` — туда падают ошибки
экспорта и вывод скриптов.

---

## Шаг 4. Сборка мопеда

1. `File → New → General` (чистая сцена — скрипт стирает всё содержимое!).
2. Вкладка **Scripting** сверху.
3. **Open** → выбрать `tools/blender_build_alpha.py` из этого репозитория.
4. **▶ Run Script** (или `Alt + P`).

В Outliner появится дерево `alphaMoped → bodyLean / wheels / player`.
В системной консоли скрипт напечатает ожидаемые пути узлов (`0>0|13|2` и т.д.).

Скрипт **специально назначает материал каждому мешу** — экспортёр ругается и
пропускает объекты без материалов.

---

## Шаг 5. Экспорт

1. Сохраните .blend файл (некоторые режимы экспорта требуют сохранённый проект).
2. Выделите корневой объект **`alphaMoped`**.
3. Боковая панель `N` → вкладка **GIANTS I3D**:
   - режим экспорта: **Selected Objects** (или Everything — в сцене только мопед);
   - путь: папка `FS19_AlphaMoped/`, имя `alphaMoped.i3d`.
4. Нажать **Export**.

Рядом с `alphaMoped.i3d` появится `alphaMoped.i3d.shapes` — **это часть модели**,
её тоже кладём в мод, без неё ничего не загрузится.

---

## Шаг 6. Сверка узлов

Откройте `alphaMoped.i3d` в **GIANTS Editor** и сверьте порядок узлов в дереве
со списком `<i3dMappings>` в `alphaMoped.xml`. Blender при экспорте может
отсортировать детей иначе, чем они создавались — тогда просто поправьте цифры
в XML (`0>0|13|2` и подобные).

Затем:

```bash
./build.sh FS19_AlphaMoped
```

и архив из `build/` — в `Документы\My Games\FarmingSimulator2019\mods\`.

---

## Если что-то пошло не так

| Симптом | Причина |
|---|---|
| Нет вкладки GIANTS I3D в панели `N` | Несовместимость версий — откатитесь на Blender 4.2 LTS |
| Аддон не виден в списке | Не включён фильтр **Community** |
| «Object has no material» | Меш без материала — экспортёр его пропустит |
| Мод не грузится, в log.txt «Failed to load i3d» | Забыли положить `.i3d.shapes` рядом с `.i3d` |
| Мопед в игре едет задом наперёд | Поменяйте `FWD` в начале `blender_build_alpha.py` |
| Кренит наружу поворота | Поменяйте знак `targetAngle` в `AlphaMopedLean.lua` |
